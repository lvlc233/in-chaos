用户对 98 引导方向未见样本 B 做正向标注：

> 好

通过版本特征：

- 承接用户已发现的“转折处”；
- 不直接给出“上下界”命名；
- 用具体树：

```text
     10
    /
   5
    \
     8
```

- 把焦点压到 `8` 同时受到哪些限制；
- 问“这两个限制分别来自哪里”，让用户自己看到路径约束。

可提取偏好：

- 当用户已发现现象但未命名结构时，引导应先帮助用户看清约束来源；
- 不要急着给术语或最终抽象；
- 具体例子比直接问“新区间是什么”更自然。
