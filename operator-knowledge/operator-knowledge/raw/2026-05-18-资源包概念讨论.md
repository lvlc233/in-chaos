# 资源包概念讨论（完整对话）

> 2026-05-18，与 agent 完整脑暴会话，原文冻结

---

user: 我打算开一个新分支内容，这是一个关于skill的概念，其实现落地我打算落在我们的ok-skill中

agent: 好的，我们先按脑暴流程来。让我先了解一下当前项目的上下文。

user: 我们现在需要讨论

---

user: 这个想法叫做资源包，也就是这个分支的名字，起源于我在做一个视频二创skill的时候遇到的问题、即agent/skill可能在某系垂直或者私人领域上的概念完全不熟悉，或者说关于某个方向上的经验不足，例如关于生图模型的个体偏好等；因此我觉得可以有一个叫做资源包的东西去专门加载这些资源；随后我有一个需求，我想做算法，并且那个时候，ok-skill的0.1版本已经出来了，思考了后，发现其实对于我来说，学习一个东西，用okskill是很好的，但是学习具体的东西，或许可以在okskill中加载对应的资源包从而做扩展，因此今天，我们要开始讨论关于资源包的事情，当前阶段，最为重要的事情是关于资源包的是否应该存在，和skill中的其他元素或者agent设计中的元素，对比是不是多余，需要研究

agent: 你说的资源包和现有的 skills 之间，你心里最关键的区别是什么？举例说，如果一个资源包里放了"某个生图模型在不同 prompt 风格下的表现偏好"，它和直接把这个知识写进一个 skill 的 SKILL.md 里，有什么本质不同？

user: skill,对我来说，他是一种抽象，而非实际工具，而有别于直接的提示词-指令，其作用是在上下文引导模型输出权重->改变模型不同领域的性能（俗称，但不是，提示词工程）的理论指引下的改变模型行为轨迹的一种方式，skill应该同人类skill一样，不是具体的某一个工具怎么使用，而是在某领域下的认知和熟练度；在经典的skill中存在一个叫ref的文件夹，该文件夹通常记录的是模板信息，或者其他和该skill相关的参考信息，他通常被认为是某个具体的功能实现；而资源包和该文件夹的概念很接近但不是，资源包是跨skill的，例如现在我有若干个skill，都是做视频的，ref内在skill中，而资源包的单位是agent；除了这一层的区别，实际上，这种知识也不应该放在skill.md中，因为这样子会使得skill膨胀，并不符合定义与概念

agent: 理解了——skill 是方法论（怎么思考/工作），资源包是领域知识（某个模型/工具的具体事实）。而且资源包的粒度是 agent 层的，跨 skill 可复用。对比验证是好的方向。你觉得在你分析过的现有机制里，哪个最接近资源包的功能？比如 opencode 的 instructions 字段，或者 MCP server？

user: 其实我觉得最接近的概念就是skill中的ref甚者说是skill，如果做个比喻的话，skill告诉你"技能"，也就是agent会什么事情的抽象，工具是工具，脚手架，老虎钳，而ref是文档说明，告诉skill和工具如何联系起来，不同的场景下如何使用，而资源包，则告诉你，当前，你可以处理什么东西，你拥有着什么产物，能够给你去处理

agent: 总结了四格对照。到"是否必要"的验证。假设没有资源包，你现在的视频二创 skill 要把那些模型偏好、领域参数硬塞进 ref 里——具体会出什么问题？是你已经碰到了哪些实际的麻烦，还是预判会膨胀失控？

user: 没有边界，无法迁移，只是agent中的某个执行环节中的某个标准，而并不是改变agent的或者说skill的整个风格表现

agent: 提了三个验证方向——A. 只用 ref 强化现状，B. 写进 instructions，C. 资源包作为一级抽象。判断是用户实际痛点在 B/C 之间——instructions 能塞但无结构、无边界、无按需控制。问用户目前有没有塞过 instructions。

user: 没有，但是思路是对的，方向C，例如现在阶段，其实如果可以，我可能会让你加载，或者你自己会主动的去加载关于skill的资源包，将skill的专业知识信息

agent: 确认方向 C，开始逐段展开设计。先出第一段：资源包的定义与边界。

user: ref和资源包的理解可能还是有问题，需要我们继续讨论，还有 instructions ，这是什么？对了，在现在这个阶段，我需要你去忽略我的描写，开subagent去调查下什么是skill，并加载相关的信息

agent: 开 subagent 研究 skill 的定义、结构、ref/references 的实质、instructions 的作用。返回了研究结果。解释了 instructions（opencode 中始终在线注入的文件）。重新整理了 ref 和资源包的区分——ref 是渐进披露机制，资源包是 agent 级的领域资产管理抽象。关键区分：skill 的契约是"跟随我"，资源包的契约是"参考我"。

user: 应该我问你，而不是你问我，，，，我的skill的理解正确吗？其实？我仍然很好奇，那这么一来，我的资源包有什么用呢？和知识库和ref和skill的差别是什么呢？那怎么实现我提到的关于算法的资源包的复刻呢？

agent: 分析了 skill 理解的正确性、资源包/ref/knowledge base/skill 的差异表，用两个用例（视频二创、算法学习）解释了资源包的实用价值——skill 管怎么学，资源包管学什么。给出了算法资源包的蓝图结构。

user: 那关于skill，我们再往深推一层，功能性skill（video-use）和业务性skill（sec-biz（这是我新的skill，你找不到，大概就是底层用的功能性skill，在上层对接业务使得用户不用关注底层原理和实现，系统根据业务功能做分配））和能力性skill（例如你加载的ok-skill和supperpower）的区别又是什么呢？

agent: 从第一性原则分析，三种 skill 的本质区别不在格式而在"修改 agent 的哪一层行为"：能力 skill 改认知层，业务 skill 改路由/翻译层，功能 skill 改执行层。依赖关系单向：能力层贯穿全栈，业务层依赖功能层。资源包不在这三层中——它不修改行为，它提供引用。

user: 先这样子吧，归档下这次的会话，还有完整的对话信息

---
