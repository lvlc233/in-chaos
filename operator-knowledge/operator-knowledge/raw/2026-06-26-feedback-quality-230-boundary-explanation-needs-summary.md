用户对 230 反馈质量拟输出 A 做局部修正。

被指出的问题：

```text
if node.left == null: return num 这个边界不对。
没有左孩子不代表当前节点不用访问；统一边界应该看当前 node 是否为空。
```

用户认为：

- 这里对“为什么边界不对”的解释不太好；
- 或许可以加一个简单总结。

含义：

- 仅说“没有左孩子不代表当前节点不用访问”还不够顺；
- 需要把错误压成一个更容易理解的判断：
  - 递归停止条件应该是“没有节点可处理”；
  - 而不是“没有左子树”；
  - 没有左子树时，当前节点仍然要作为中序里的“当前”被访问。

待后续修正：

- 对边界错误要说明“停止对象”是什么；
- 对树递归来说，停止的是空节点，不是某个孩子为空。
