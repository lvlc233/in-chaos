用户对 148 引导方向未见样本 B 做正向标注：

> 好，pass

通过版本特征：

- 下一步焦点是“子问题到底是什么”；
- 说明原因：用户已会找中点，也知道合并有序链表，缺的是识别原问题能变成同类小问题；
- 使用具体链表例子 `4 -> 2 -> 1 -> 3`；
- 不直接告诉用户“这是分治”；
- 通过比较“排序一条短链表”和“排序原链表”是不是同一种问题，引导用户看到同构小问题。

可提取偏好：

- 对算法范式识别问题，引导应让用户看见“子问题与原问题是否同构”；
- 可以给具体输入拆分；
- 不急着命名范式。
