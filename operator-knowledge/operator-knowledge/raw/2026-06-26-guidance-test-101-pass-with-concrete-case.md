用户对 101 引导方向修正版做正向标注：

> ok可以；pass

通过版本特征：

- 先指出下一步看 `duic(tree_left, tree_right)` 的入口判断；
- 说明原因：外侧/内侧递归方向已对，真正影响结果的是入口处什么时候停、什么时候失败、什么时候继续；
- 不直接列出三种边界情况的答案；
- 给具体最小树例子：

```text
  1
 / \
2   2
```

- 用 `duic(null, null)` 让用户自己判断返回含义。

可提取偏好：

- 引导可以给具体案例；
- 具体案例不等于直接给答案；
- 对递归边界问题，具体小树比抽象描述更自然。
