用户指出未见样本测试需要用户原话。

问题：

- agent 给 98 样本做反馈质量测试时，只用了记录中的概括；
- 用户追问：“用户当初说了什么？有用户原话吗？”
- 这暴露了测试设计缺口：如果没有用户原话，就无法验证输出是否真正贴用户输入。

修正：

- 未见样本应尽量保留用户原始说法；
- 至少要有近原文片段；
- 不能只用 records 中的总结性描述；
- 否则“贴用户具体说法”这个反馈质量维度无法被可靠测试。

待后续采用：

- 每个测试样本包含：
  - 用户原话；
  - 题目上下文；
  - 已知卡点；
  - agent 双份输出；
  - 用户偏好标注。
