用户提出新的训练偏好：

> 案例是最危险的订正，尽可能不要使用案例，或者专门存储案例而非随意使用；等下一轮吧。

含义：

- 具体案例虽然能改善某些输出；
- 但如果把案例直接写进 skill，agent 可能机械模仿案例；
- 案例容易成为过拟合点；
- 案例不应随意塞进主规则；
- 如果需要案例，应专门存储为案例资源，而不是写成默认行为。

当前影响：

- 刚刚在 algo-coach SKILL.md 中加入递归边界引导示例，可能不合适；
- 应改成原则化行为，或把案例放到单独资源中，按需读取；
- 当前轮先修正，不继续测试。

待后续：

- 区分“规则”和“案例资源”；
- skill 主体中尽量写触发条件与行为原则；
- 案例单独存储，避免污染默认输出风格。
