用户对未见样本 101 的双份输出做标注。

总体：

- 反馈质量 v0.1 风格输出整体不错；
- 但有两个问题需要修正。

问题 1：空节点边界的指代不清

原句：

```text
空节点边界没覆盖完整。两个都为空时应成立；只有一个为空时应失败。现在这两种情况没有稳定区分。
```

用户疑问：

- “空节点”是在指什么？
- “两个”是指哪两个？
- 这句话没有贴到用户的变量名或函数参数；
- 用户需要知道这里说的是 `duic(tree_left, tree_right)` 里的这两个节点。

修正方向：

- 明确说：

```text
这里说的是 duic(tree_left, tree_right) 接收到的这两个节点。
```

问题 2：已知小问题的处理方式

原句：

```text
最外层调用 duic(tree.left, tree.right) 后，也需要返回它的结果，否则辅助函数判断出来的真假没有传回主函数。
```

用户指出：

- 这属于之前说过的小问题处理方式；
- agent 需要识别；
- 如果是明显的小问题/已知问题，处理强度应降低。

待后续整理：

- 反馈中的代词和抽象对象必须绑定用户变量；
- “两个节点/空节点”这类说法要指明是哪两个；
- 对已知小问题或低阶漏写，轻提醒即可，不要和核心问题等权。
