用户对当前过程作出类比：

> 人类偏好的数据标注环节啊……
> 是也不是，应该算人类对齐的后训练过程了。

含义：

- 当前不是单纯写一个 skill；
- 也不是简单把用户想法记录下来；
- 更像是在通过用户反馈对 agent 行为进行偏好标注；
- 用户指出哪些输出好、哪些不好、为什么不好；
- agent 根据这些偏好逐步修正交互模式；
- 这类似人类偏好数据收集 / 对齐 / 后训练过程。

注意：

- 这里不是严格意义上的模型参数训练；
- 但在 skill 和交互规则层面，确实是在做“行为后训练”；
- 用户的偏好反馈成为行为规则和测试样本的来源。

待后续整理：

- 可以把正例、逆例、失败观察、修正规则都看作偏好数据；
- skill 迭代不仅是规则编写，也包括偏好样本收集和行为校准。
