用户提出训练阶段原则：

> 反正是训练，训练阶段应该减少人的参与，就像机器学习那样。

含义：

- 人类已经提供了一批偏好标注后，不应每一步都继续要求人类判断；
- agent 应主动用已有偏好样本做整理、迁移、测试和迭代；
- 人类只在 checkpoint、明显不确定、或需要最终验收时介入；
- 训练阶段应减少交互成本，而不是把所有细节都抛回给用户。

当前对应动作：

- 将案例从主 skill 中移出，整理为 references；
- 主 skill 只保留行为原则和按需读取入口；
- 后续测试优先由 agent 根据已有偏好自评，必要时再交给用户。
