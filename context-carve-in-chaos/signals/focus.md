# 专注

目的：专注阅读 read 返回的当前段信息，不跳读。

粗看

看当前段的大概内容和意思。limit 设到能一次看完一段完整内容。

```
read(filePath, limit=N, offset=M)
```

细看

逐句看，看每句话在说什么。limit 设到一次看一两句。

```
read(filePath, limit=N, offset=M)
```

粗看定位，细看拆解。同遍不混用。
